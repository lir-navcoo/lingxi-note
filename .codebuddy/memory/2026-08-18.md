# 2026-08-18

- 修复登录后侧边栏仍显示“未登录”：根因是客户端无法读取 `HttpOnly` 的 `lx_session` Cookie；首页与编辑器页现从 `/api/auth/me` 获取 `SessionUser` 并传给 `TocSidebar`。
- 根布局 `body` 改用 `min-h-svh`，与页面 `h-svh` 配合确保视口满高。
- 删除无引用且不适用于 `HttpOnly` Cookie 的 `readSessionUserClient()`；相关文件 lint 通过。
- 修复编辑器页面比视口高约 36px：根因是 `PlateEditor` 的 `h-9` 保存栏与 `h-full` 编辑器纵向相加。现使用满高纵向 flex 容器，编辑器以 `min-h-0 flex-1` 占据剩余高度；右侧页面容器增加 `overflow-hidden`，相关 lint 通过。
- 排查 UploadThing 上传 500：请求已通过代理并命中路由，根因是运行环境缺少 `UPLOADTHING_TOKEN`。已在 `.env.local.example` 补充配置项、增加服务端缺失凭据提示，并移除上传失败后伪装成功的 Blob mock；同时修复上传成功时返回旧 state 的问题。实际上传需配置有效 Token 后重启服务。
- 重写 `README.md`：移除默认 Next.js 模板，补充项目功能、技术栈、完整环境变量、本地开发与 8081 端口、数据库初始化顺序、自建 JWT 鉴权、主要路由/API、UploadThing 配置及项目结构。
- 优化编辑器文字颜色/背景颜色共用选择弹窗：宽度由 280px 调整为 360px（小屏限制为视口减 16px），保持 10 列色板；色块由 24px 放大至 28px，自定义颜色按钮增至 36px，并移除弹窗阴影。相关 lint 通过。
- 排查 `/preview` 404 与图片上传：新增 `app/preview/page.tsx` 作为兼容入口重定向到首页；确认当前 `.env.local` 缺少 `UPLOADTHING_TOKEN`，上传失败是环境变量未配置导致，需填入有效 Token 后重启开发服务器。全量 lint 仍有项目既有错误，新增及上传相关文件无诊断。
- 增加错误反馈：`/preview` 使用 Dialog 说明独立预览限制并通过 Sonner 提示后返回首页；上传 Hook 针对 Token/鉴权错误显示配置提示，媒体上传错误按文件类型、大小和数量显示中文 Sonner 反馈。相关文件 lint 无诊断。
- 将根布局的 favicon、shortcut 和 Apple 图标元数据统一指向现有 `app/favicon.svg`，避免浏览器或设备继续使用默认图标；`app/layout.tsx` lint 通过。
- 按用户选择将文件上传从 UploadThing 迁移到本地 Supabase Storage：新增 `app/api/upload/route.ts`，按用户 ID 保存文件并返回公开 URL；`hooks/use-upload-file.ts` 改为 XHR 上传并保留进度及 Sonner 提示；移除 UploadThing 路由、配置、依赖和设置项，新增 `SUPABASE_STORAGE_BUCKET` 配置说明。新增/修改上传相关文件 lint 通过，TypeScript 仅剩项目既有错误。
- 修复本地 Supabase Storage 上传后预览失败：上传接口会在目标桶已存在但为私有时自动调用 `updateBucket` 切换为公开，并在 `.env.local` 补齐 `SUPABASE_STORAGE_BUCKET=note-media`；上传路由 lint 通过。
- 将编辑器右键菜单中的 AI 助手、删除、创建副本、块类型、缩进和对齐选项统一改为中文；修复顶部对齐下拉框因 `min-w-0` 导致宽度被压缩，改为自适应且最小宽度 `128px`；相关文件 lint 通过。
- 将编辑器正文占位提示 `Type something...` 改为“输入内容...”，并将媒体工具栏的 `Edit link`、`Caption` 改为“编辑链接”、“添加说明”；相关文件 lint 通过。
- 将编辑器块和分栏拖动手柄的 `Drag to move`、`Drag to move column` 提示改为“拖动以移动”、“拖动以移动分栏”；相关文件 lint 通过。
- 新增动态路由 `app/preview/[id]/page.tsx`，支持通过 `/preview/{noteId}` 免登录只读展示 Plate 富文本内容；`proxy.ts` 已放行 `/preview`。实测未登录请求不再 307 跳转登录，未知 ID 正确返回 404；预览页将正文首个标题块与其余正文分段渲染，使更新时间位于标题下方且不重复标题；该页 lint 通过，项目 TypeScript 仍有既有错误。
- 预览页新增响应式文档目录：从正文标题提取目录项，自动补充锚点 ID，桌面端侧栏显示层级缩进，移动端改为正文上方的目录区域；目录链接可跳转至对应标题，页面保持亮色无阴影样式。
- 编辑模式右上角工具栏已移除高亮、评论和模式切换按钮，右侧不再保留任何按钮；`fixed-toolbar-buttons.tsx` lint 通过。
- 预览页外层容器移除默认的 `py-12` 顶部/底部间距，保留响应式 `sm:py-16`。
- 修复预览文章目录混入非当前用户或历史文章：目录按当前文章 `user_id` 隔离，并通过 `revalidate = 0` 确保删除后读取最新数据。
- 优化 `README.md` 顶部 GitHub 项目介绍，补充灵犀笔记的产品定位、项目亮点和核心能力，保留原有安装配置文档。
- 在 `README.md` 增加完整部署手册，覆盖部署前准备、生产环境变量、Vercel、自建 Node.js + PM2 + Nginx 部署、安全要求及上线验证清单。
- 按后续要求移除预览页外层容器的 `sm:py-16`，现在不再设置纵向 padding；`app/preview/[id]/page.tsx` lint 通过。
- 修复浏览器顶部标签页图标：确认 `/favicon.svg` 实际返回 404，改用 Next.js 16 支持的 `app/icon.svg` 文件约定，并同步更新根布局图标元数据；SVG 与 lint 校验通过。
- Docker 部署改为应用与本地 Supabase 一体化 Compose：加入 PostgreSQL、PostgREST、Storage、Kong、自动数据库/Storage 权限初始化、持久化 Volume 和本地密钥生成脚本；REST 与 Storage API 均实际验证成功。服务端 Supabase 支持 `SUPABASE_INTERNAL_URL`。当前 Docker 应用镜像构建因 Docker 内存不足在 Next.js 编译阶段被终止，README 已注明至少分配 4 GB。
- `README.md` 的 Docker Compose 部署章节已补充服务器配置建议：最低 2 核/4 GB/10 GB，推荐 4 核/8 GB/20 GB 以上，并说明构建与运行内存、磁盘增长、Docker Desktop 资源、端口暴露、HTTPS 和备份要求。
