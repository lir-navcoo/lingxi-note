# 2026-08-13 工作日志

## 构建/运行错误修复
- 修复 `Module not found: @radix-ui/react-dropdown-menu`：pnpm add 安装了 `@radix-ui/react-dropdown-menu` 与 `@radix-ui/react-tooltip`（原 package.json 缺这两个）
- 修复 `Tooltip must be used within TooltipProvider`：在 `app/page.tsx` 顶层用 `TooltipProvider` 包裹 `PlateEditor`（toolbar.tsx 的 withTooltip 使用 Radix Tooltip 但自身未包 Provider）

## 启动「正式优化」项目
- 用户确认方向：代码质量与架构 + 功能完善 + 视觉与交互体验，按模块逐步、每步可见可验收
- 第一个模块定为「编辑器核心」，已扫描 plate-editor.tsx / editor-kit.tsx / settings-dialog.tsx / editor-base-kit.tsx / app 入口
- 待产出第一阶段优化提案与用户确认

## 顶部 Header 与中文化改造（用户逐步指导）
- 新建 `components/site-header.tsx`：灵犀笔记中文标题栏（Lucide BookOpenText 图标，亮色无阴影）；右上角加用户预留位（Lucide User 图标按钮）
- `app/page.tsx`：接入 SiteHeader，布局改为 h-svh flex-col，编辑器占满
- `app/layout.tsx`：metadata 改为「灵犀笔记」、lang=zh-CN、移除全局 font-mono
- `app/globals.css`：默认字体 font-mono → font-sans（中文友好）
- `components/editor/plate-editor.tsx`：示例文章全部中文化，移除 emoji（✨/✅ 改为「支持」文字）
- 新建 `components/toc-sidebar.tsx`：左侧目录侧边栏，复用 @platejs/toc 的 useTocElementState/useTocElement，点击平滑滚动定位 + active 高亮，亮色无阴影
- `plate-editor.tsx` 改为 flex 布局：左 TocSidebar + 右 EditorContainer(variant=default h-full)；editor-kit 中 TocKit 已启用，无需改插件
- 视觉约束核对：侧边栏白底 border-slate-200、active bg-slate-100，均符合无阴影/亮色/无 emoji 偏好

## 编辑器全量中文化（界面文案）
- 中文化范围：components/ui/ 下所有工具栏按钮 tooltip、下拉菜单项 label/group、对话框标题/描述/按钮/placeholder/空状态、AI 菜单 label、评论弹窗、设置对话框、slash 命令、emoji/mention 空状态、媒体/表格/代码块节点占位符
- 已处理文件（约 25+）：fixed-toolbar-buttons、floating-toolbar-buttons、turn-into、insert、more、link、link-toolbar、history、indent、align、toggle、line-height、equation、suggestion、comment、emoji-toolbar-button、font-color、mode、media-*、table-*、slash-node、ai-menu、comment、settings-dialog、code-block-node、code-drawing-node 等
- 修正的视觉违规：settings-dialog 第 333 行 shadow-md → 改 border+hover:bg-slate-50；第 352 行 dark:bg-purple-900 → 移除；table-node 浮层 shadow-md → 移除
- 保留英文（有意不翻译）：AI prompt（发给模型的英文指令）、模型名（GPT-5 等）、emoji 库内部 i18n、颜色名枚举（约 80 个，属数据非界面文案）、equation 占位符 E=mc^2（公式输入框示例）
- 搜索关键词 keywords 保留英文（不影响搜索功能）
- lint 检查通过（0 错误）

## 左侧菜单重新定位 + Supabase 一体化接入
- 用户明确：左侧不是「目录」，是「文章」菜单（预留→已接真实数据）。`components/toc-sidebar.tsx` 从 @platejs/toc 目录改为文章列表（读 notes 表、新建/删除/选中高亮），标题「文章」，右上角 + 新建
- 选型：放弃 localStorage，用本地 Docker Supabase（用户已装，Colima 运行时）。一体化 = Next.js 单仓库 + Supabase 提供 PG/Auth/Storage/Realtime
- 端口调整：Next dev/start 用 `-p 8081`（Next16 Turbopack 的 next dev 不读 .env 的 PORT，必须用 -p）；Supabase Studio 由 3001 改到 3000（改 docker-compose.override.yml 的 studio ports 为 3000:3000 并 docker compose up -d studio）
- 本地 Supabase 信息：Kong API URL `http://localhost:8000`，anon key 在 `/Users/lirui/Documents/projects/supabase/docker/.env` 的 ANON_KEY（固定开发 JWT），已写入灵犀笔记 `.env.local`（NEXT_PUBLIC_SUPABASE_URL/PULISHABLE_KEY）
- 建 Supabase 客户端层：`lib/supabase/{client,server,proxy}.ts` + `middleware.ts` + `database.ts`（notes 表类型）；`supabase/schema.sql` 建 notes 表（id/title/content jsonb/parent_id/position/icon/user_id/时间戳，索引+更新时间触发器+开发期开放 RLS notes_dev_open）
- 多页面闭环：新增 `lib/notes.ts`（list/create/updateTitle/updateContent/delete）；`app/page.tsx` 改为客户端组件管理 notes+currentId；`plate-editor.tsx` 接收 noteId/initialValue，onChange 防抖 800ms 存 content+提取首行作 title，key={noteId} 切换重建；验证全链路读写通、首頁 8081 编译 200
- 注意：Next16 的 middleware.ts 已 deprecated，建议迁移为 proxy.ts（未做）；RLS 当前开发期全开放，未接 Auth
- 本地 Supabase 密码（PG 5432）：见 supabase/docker/.env POSTGRES_PASSWORD；JWT_SECRET 为固定开发值，仅限本地，上线前须换

## 接 Auth（用户选择方向 1）
- 登录/注册闭环完成：新建 `components/auth/auth-form.tsx`（亮色无阴影无 emoji，邮箱+密码，注册后自动登录，window.location.reload 触发会话刷新）、`app/login/page.tsx` 登录页
- `app/page.tsx`：进入即校验 `auth.getUser()`，未登录 `window.location.href='/login'`；底部用户区加 `onSignOut` 登出（signOut 后跳 /login）
- `components/toc-sidebar.tsx`：导出 `Logo`（内联 SVG，登录页复用）；底部用户区未登录可点跳转登录、已登录显示 LogOut 图标登出
- 数据隔离：`lib/notes.ts` 的 `listNotes` 仅列 `user_id=auth.uid() OR user_id IS NULL`（兼容遗留），`createNote` 写入 `user_id=auth.uid()`
- RLS 收紧：`supabase/rls.sql` 替换开发期全开放策略 → `notes_owner_all`（authenticated 读写自己）+ `notes_legacy_read`（anon/authenticated 只读 user_id IS NULL 的遗留文章，避免旧数据消失）。已在本地 supabase-db 执行（docker exec psql 跑 rls.sql，notes_dev_open 已 drop）
- 本地 Auth 配置：`supabase/docker/.env` 的 `ENABLE_EMAIL_AUTOCONFIRM` 由 false 改为 true（开发期免邮箱确认，密码注册直接可登录），已 `docker restart supabase-auth` 生效
- 下一步可选：middleware.ts→proxy.ts 迁移；/editor/[id] 独立路由；旧 NULL 文章认领

## 方向 2：/editor/[id] 独立路由（用户确认）
- 用户明确方向 2 = 文章独立路由。已完成：
- 新建 `app/editor/[id]/page.tsx`：全屏编辑页，读 `params.id`（Next16 动态路由 params 为 Promise，用 React.use() 解包），加载文章 `getNote(id)` + 列表 `listNotes()`，未登录跳 /login，文章不存在/无权访问显示占位；左侧 TocSidebar 切换/新建/删除，onSelect/onCreate 走 `router.push('/editor/'+id)`
- `app/page.tsx` 改为纯列表首页：只加载 notes、渲染 TocSidebar + 引导文案，选中/新建跳 `/editor/[id]`，不再内嵌 PlateEditor
- 删除旧的 `app/editor/page.tsx`（无 id 占位页，避免与 [id] 路由混淆）
- `lib/notes.ts` 新增 `getNote(id)`：select 单篇，按 RLS 仅自己或 user_id IS NULL
- 视觉/约束保持：亮色无阴影无 emoji；TocSidebar 已有 Logo/用户区，独立路由页复用同一组件
- 验证：tsc 本次改动文件 0 错误；dev(8081) 首页与 /editor/test-id 均 200
- 现状：文章有独立可分享/可刷新 URL，刷新不丢定位；首页与编辑页分离，架构清晰
- 仍待办（非本次）：middleware→proxy 迁移、旧 NULL 文章认领给当前用户

## 方向 B：自建多用户鉴权（替代 Supabase Auth，用户明确选择）
- 用户不满 Supabase Auth（注册要发邮件、auth 容器不稳），决定自建鉴权。采用方案 B（多用户）。
- 已彻底脱离 Supabase Auth：注册/登录/登出/密码/会话 100% 自建，不发邮件。
- 新增文件：
  - `supabase/users.sql`：自建 `users` 表（id/email unique/password_hash/name/时间戳），已 docker exec psql 执行建表成功
  - `lib/auth/password.ts`：scrypt 哈希+校验（node:crypto，零依赖，timingSafeEqual）
  - `lib/auth/jwt.ts`：手写 HS256 会话 JWT 签发/校验（node:crypto，零依赖）
  - `lib/auth/session.ts`：`SESSION_COOKIE='lx_session'`、`SESSION_MAX_AGE=7d`；服务端 `getSessionUser()`（next/headers+cookies 验签），客户端 `readSessionUserClient()`（仅读 payload 不验签）
  - `lib/auth/db.ts`：users 表增查（service_role）
  - `lib/supabase/admin.ts`：service_role 管理员客户端（绕过 RLS，仅后端用）
  - `app/api/auth/{register,login,logout,me}/route.ts`：注册/登录/登出/当前用户
  - `app/api/notes/route.ts`（GET list / POST create）、`app/api/notes/[id]/route.ts`（GET/PUT/DELETE）：后端用 service_role + `where user_id=session.userId` 应用层隔离
- 改造：
  - `lib/notes.ts`：浏览器不再直连 Supabase，改为 fetch 自建 API（credentials same-origin 带 cookie）；导出签名不变（listNotes/getNote/createNote/updateNoteTitle/updateNoteContent/deleteNote）
  - `app/page.tsx`、`app/editor/[id]/page.tsx`：登录校验改 `/api/auth/me`，登出改 `/api/auth/logout`
  - `components/toc-sidebar.tsx`：用户信息改 `readSessionUserClient()`
  - `components/auth/auth-form.tsx`：去掉 Supabase，调 `/api/auth/login|register`，错误用后端中文提示
  - `middleware.ts` → `proxy.ts`：Next16 弃用 middleware 改名 proxy；proxy 默认 Node runtime（node:crypto 可用），校验 session cookie + 未登录跳 /login + 续期
  - 删除：`lib/supabase/{client,server,proxy}.ts`（不再使用）；保留 `admin.ts`+`database.ts`
- 配置 `.env.local`：加 `SUPABASE_SERVICE_ROLE_KEY`（本地真实 service_role，仅服务端用，无 NEXT_PUBLIC 前缀）、`AUTH_JWT_SECRET`（本地 supabase JWT_SECRET）
- RLS 现状：`auth.uid()` 已不再适用（无 Supabase Auth 会话）；数据隔离改由后端应用层 `where user_id` 保证；service_role 绕过 RLS。`supabase/rls.sql` 的旧策略对直连浏览器已无效，但浏览器不再直连，无安全风险。 notes 表 user_id 现由自建 users.id 填充
- 端到端验证全部通过（curl）：未登录首页 307→/login；注册 200 不发邮件；me 200；建文 user_id 正确；列表仅自己；错误密码 401；用户 B 列表空且读 A 文章 404；重复注册 409；带会话首页/编辑器 200；登录页 200
- 注意：旧 user_id=NULL 的遗留文章仍可被 service_role 看到，但当前 API 只按 `user_id=session.userId` 查，遗留 NULL 文章对任何新用户都不可见（需用 update 认领到某用户）。这是已知待办（旧 NULL 文章认领）。
